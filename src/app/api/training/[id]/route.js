import { NextResponse } from "next/server";
import { requirePermission } from "@/lib/auth/requireAuth.js";
import { getTrainingById, updateTraining, deleteTraining } from "@/services/training/training.service.js";
export async function GET(request,context) { try { const a=await requirePermission(request,"TRAINING_VIEW");if(!a.success)return a.response;const p=await context.params;const record=await getTrainingById({user:a.user,id:p?.id});return NextResponse.json({success:true,record}); }catch(e){return NextResponse.json({success:false,message:e?.message||"Unable to load training."},{status:e?.statusCode||e?.status||500});} }
export async function PUT(request,context) { try { const a=await requirePermission(request,"TRAINING_UPDATE");if(!a.success)return a.response;const p=await context.params;const record=await updateTraining({user:a.user,id:p?.id,data:await request.json()});return NextResponse.json({success:true,record}); }catch(e){return NextResponse.json({success:false,message:e?.message||"Unable to update training."},{status:e?.statusCode||e?.status||500});} }
export async function DELETE(request,context) { try { const a=await requirePermission(request,"TRAINING_DELETE");if(!a.success)return a.response;const p=await context.params;const result=await deleteTraining({user:a.user,id:p?.id});return NextResponse.json(result); }catch(e){return NextResponse.json({success:false,message:e?.message||"Unable to delete training."},{status:e?.statusCode||e?.status||500});} }
