import { NextResponse } from "next/server";
import { requirePermission } from "@/lib/auth/requireAuth.js";
import { getSupplierById, updateSupplier, deleteSupplier } from "@/services/suppliers/supplier.service.js";
export async function GET(request,context) { try { const a=await requirePermission(request,"SUPPLIER_VIEW");if(!a.success)return a.response;const p=await context.params;const record=await getSupplierById({user:a.user,id:p?.id});return NextResponse.json({success:true,record}); }catch(e){return NextResponse.json({success:false,message:e?.message||"Unable to load supplier."},{status:e?.statusCode||e?.status||500});} }
export async function PUT(request,context) { try { const a=await requirePermission(request,"SUPPLIER_UPDATE");if(!a.success)return a.response;const p=await context.params;const record=await updateSupplier({user:a.user,id:p?.id,data:await request.json()});return NextResponse.json({success:true,record}); }catch(e){return NextResponse.json({success:false,message:e?.message||"Unable to update supplier."},{status:e?.statusCode||e?.status||500});} }
export async function DELETE(request,context) { try { const a=await requirePermission(request,"SUPPLIER_DELETE");if(!a.success)return a.response;const p=await context.params;const result=await deleteSupplier({user:a.user,id:p?.id});return NextResponse.json(result); }catch(e){return NextResponse.json({success:false,message:e?.message||"Unable to delete supplier."},{status:e?.statusCode||e?.status||500});} }
