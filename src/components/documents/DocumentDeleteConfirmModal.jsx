"use client";
export default function DocumentDeleteConfirmModal({ open, document, onClose, onConfirm, loading = false }) { if (!open || !document) return null; return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4"><div className="w-full max-w-md rounded-2xl bg-white p-6 shadow-xl"><h2 className="text-lg font-semibold">Delete Document?</h2><p className="mt-2 text-sm text-slate-600">This will permanently delete <b>{document.documentNumber}</b> — {document.title}.</p><div className="mt-6 flex justify-end gap-3"><button onClick={onClose} disabled={loading} className="rounded-xl border px-4 py-2.5">Cancel</button><button onClick={onConfirm} disabled={loading} className="rounded-xl bg-red-600 px-4 py-2.5 text-white">{loading ? "Deleting..." : "Delete"}</button></div></div></div>; }
